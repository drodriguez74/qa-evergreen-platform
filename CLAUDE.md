# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**All agent guidance lives in [`AGENTS.md`](AGENTS.md)** — the tool-agnostic source of truth (golden
rules, architecture, the onboard-an-app flow, working-on-the-platform commands, conventions, and
what-not-to-do). Read it. This pointer exists so the two don't drift; do not duplicate guidance here.
